# Changelog

## Unreleased

- Initial standalone `chain-signer` implementation scaffold.
- Added a Vault external plugin backend with typed `/v1` endpoints.
- Added a generic Go client, policy engine, key storage, and EVM/TRON signing and recovery logic.
